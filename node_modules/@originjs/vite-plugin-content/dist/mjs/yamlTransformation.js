import YAML from 'js-yaml';
import { createFilter } from '@rollup/pluginutils';
import toSource from 'tosource';
export default function yamlTransform(options = {}, code, id) {
    const filter = createFilter(options.yaml.include, options.yaml.exclude);
    if (!filter(id)) {
        return null;
    }
    let yamlData;
    if (options.yaml.loadMultiDocument) {
        yamlData = YAML.loadAll(code);
    }
    else {
        yamlData = YAML.load(code);
    }
    const generatedCode = `var data = ${toSource(yamlData)};\nexport default data;`;
    return {
        code: generatedCode,
    };
}
//# sourceMappingURL=yamlTransformation.js.map